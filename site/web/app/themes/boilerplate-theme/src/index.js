import jQuery from 'jquery';
import './sass/style.scss';

// ('use strict');
(function test() {
  jQuery(() => {
    // console.log('Document Ready test');
    const a = 10;
    return a;
  });
}(jQuery));
